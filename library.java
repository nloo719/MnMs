public class library{

  public library(){
    // Nothing happens. 
  }

 /* 
  public void badEvents(int stress){
    double roll = Math.random();
    switch(roll){
      case (roll <=0.01):
        // Set stress to 100
        break;
      case (roll > 0.01 && roll <= 0.02):
        // Set test to lvl 5. Decrease time by 20s
        break;
      case (roll > 0.02 && roll <= 0.12):
        // + 10% stress
        break;
      case (roll > 0.12 && roll <= 0.22):
        // Test difficulty +1
        break;
      case (roll > 0.22 && roll <= 0.32):
        // Test time decreased -10s
        break;
      default:
        stress += 5;
    }
}
*/
}
